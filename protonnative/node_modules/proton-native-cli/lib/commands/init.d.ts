import Base from "../base";
export default class Init extends Base {
    static flags: {
        verbose: import("@oclif/parser/lib/flags").IBooleanFlag<boolean>;
    };
    static args: any[];
    static description: string;
    macWarning(): void;
    run(): Promise<void>;
    installDeps(deps: string[], saveType: string, verbose: boolean): Promise<unknown>;
    printSuccessMessage(rootPath: string, projectName: string): void;
}
